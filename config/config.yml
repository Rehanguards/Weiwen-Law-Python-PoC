models:
  - type: main
    engine: openai
    model: llama3.2:latest
    parameters:
      base_url: "http://localhost:11434/v1"
      api_key: "ollama"

rails:
  input:
    flows:
      - self check input

  output:
    flows:
      - self check output